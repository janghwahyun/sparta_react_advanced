import React from 'react';
import styled from 'styled-components';



const Text = (props) => (

	const {children, bold, color, size} =props;

	<></>		
	);

Text.defaultProps = {
	children : null,
	bold:false,
	color: #ff00c8,
	size: '14px',
};


export default Text;